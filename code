# BlackFri
input Year output BlackFri
from datetime import date,datetime
year = int(input('require year: '))
for month in range(1,13):
   if date(year,month,13).weekday() == 4:
      print(date(year,month,13))
